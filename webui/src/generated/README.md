# Generated OpenAPI types

Run with the API listening locally:

```bash
cd webui
npm run openapi:types
```

Output: `openapi.d.ts` (gitignored optional — regenerate when API shapes change).

Set `HOMEBOT_OPENAPI_URL` to point at another host.
